# NOTE: This is an example — never commit real secrets.
# Generate base64 values with: echo -n 'value' | base64
# In production use Sealed Secrets, External Secrets Operator, or Vault.
apiVersion: v1
kind: Secret
metadata:
  name: task-management-secrets
  namespace: task-management
type: Opaque
data:
  # echo -n 'change-me-in-production' | base64
  JWT_SECRET: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
